# ┌───────────────────────────────────────────────────────────────────
# │ 패키지 메타데이터
# └───────────────────────────────────────────────────────────────────
[package]
name = "cdr_file_handler" # 패키지 이름 (프로젝트 식별자)
version = "0.1.0" # 버전 (SemVer)
edition = "2024" # Rust 에디션 (최신 기능 사용)

# ┌───────────────────────────────────────────────────────────────────
# │ 외부 크레이트(라이브러리) 의존성
# └───────────────────────────────────────────────────────────────────
[dependencies]
notify = "8.0.0" # 📁 파일/디렉토리 변경 감지
chrono = "0.4.41" # 시간 처리